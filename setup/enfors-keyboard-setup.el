;;; My own keyboard stuff
(defun ce-ins-tilde ()
  "Insert ~ at point."
  (interactive)
  (insert "~"))

(defun ce-ins-caret ()
  "Insert ^ at point."
  (interactive)
  (insert "^"))

(provide 'enfors-keyboard-setup)
